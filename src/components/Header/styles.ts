import { Box, styled } from "@mui/material"

export const Container = styled(Box)(({ theme }) => ({
  padding: theme.spacing(4),
  textAlign: "center",
  // fontSize: "2px",
  // background: "blue",
}))

